version: "3.9"
services:
  web:
    build: ./web
    restart: unless-stopped
    ports:
      - "3000:3000"
  api:
    build: ./api
    restart: unless-stopped
    depends_on:
      - db
  metrics:
    build: ./metrics
    restart: unless-stopped
    depends_on:
      - db
  infra:
    build: ./infra
    restart: unless-stopped
    depends_on:
      - db
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: dev
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
